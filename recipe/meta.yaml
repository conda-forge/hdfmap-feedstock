{% set python_min = "3.10" %}
{% set name = "hdfmap" %}
{% set version = "1.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/hdfmap-{{ version }}.tar.gz
  sha256: 57693f68371cb7a659f60fb08ab1b3108597ba427b95db1cd164ee1fd0f3138d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61.0
    - wheel
    - pytest
    - numpy
    - h5py
    - hdf5plugin
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - h5py
    - hdf5plugin
    - asteval

test:
  imports:
    - hdfmap
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  summary: Map objects within a HDF5 file and create a dataset namespace
  home: https://github.com/DiamondLightSource/hdfmap
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - DanPorter
    - PeterC-DLS
